name: form_inputs
description: >-
  This is a library for basicly designing authentications forms.
  It can change over the app developement.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.5

# Add regular dependencies here.
dependencies:
  formz: ^0.4.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - Sign In
  - Sign Up
  - Sign Out
  - Reset Password
  - Update Password
  - Authentication
